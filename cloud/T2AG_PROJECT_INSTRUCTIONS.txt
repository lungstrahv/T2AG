T2AG CLOUD PROJECT INSTRUCTIONS
protocol_version: T2AG-CLOUD-1
cloud_project_mode: generic_skeleton

本文件只定义空 Skeleton 的云端边界，不启动真实云端教学。

1. Skeleton 不含真实学生、课程、group、教师映射或 base state。
2. `main/40_course/<COURSE_ID>/progress.md` 才能成为实例化后的进度真相源。
3. 未完成首次启动前，不得生成教学 receipt、session close、身份映射或同步指令。
4. Cloud bridge 在 0.2.0 换代期间保持 `paused`；恢复必须由实例化后的用户明确确认。
5. 不得从 Main、Lite、历史 lesson 或示例推断个人资料。
6. 不得自动安装依赖、下载教材或创建、删除、重建、升级 `.venv`。
