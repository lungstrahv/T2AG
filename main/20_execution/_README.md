# 执行绑定共同容器 目录说明

> **对象职责**：G（刚性课程组）和 R（弹性执行绑定）的共同父容器
> **所有者**：Case（执行选择）
> **ID 格式**：G: `GNN`；R: `RNNN`
> **真相源**：各 G/R 文件自身
> **允许引用**：CourseRun ID
> **禁止**：不拥有或复制课程内容、进度、lesson
> **迁移状态**：结构准备批次建立的空骨架；当前 G 实例仍在 `20_groups/`（兼容期）
> **本批次为空骨架**：是

## 子目录

- `groups/`：G（刚性课程组）目标容器
- `bindings/`：R（弹性执行绑定）目标容器

## 兼容期说明

当前 G 实例仍位于 `20_groups/`，迁移完成前继续有效。
本目录不承载实例真相，直到单对象迁移批次执行。

> 本批次为空骨架。当前无实例。迁移完成前旧路径继续有效。
