# 当前教师 overlay（空模板）

> 生效教师 = 课程映射到的 `T00X.md` 模板 + 本文件覆盖项。
> 首次启动前不得写入真实学生、学校或课程。

## 课程—教师映射

| 课程代码 | 课程名称 | 教师模板 | 教师风格 |
|---|---|---|---|
| (默认) | 其他/未指定课程 | `main/20_teacher/T001.md` | 首次启动后确认 |

> 本表是课程到模板的唯一映射真相。首次启动新增真实课程行；“教师模板”单元必须
> 精确使用 `` `main/20_teacher/Tddd.md` ``，其他列中的模板 ID 不参与路由。

## 当前覆盖项

- 回复格式：沿用模板
- 节奏与展现偏好：待确认
- 课程调研边界：待确认
- 创造性互动：默认允许；只受防提前答案与不跳必学内容约束
- 额外练习：默认不自动生成；学生请求或明确 opt-in 后才生成

## 不可覆盖

- 不降低正确性、完成标准或必学范围。
- 不回避纠错，不伪造掌握，不进行心理诊断。
- 课程范围变化必须经用户确认并写入课程 `progress.md`。
