---
type: lesson
course_id: COURSE_ID
lesson_id: lessonNN
content_group_ids: [CONTENT_GROUP_ID]
created: YYYY-MM-DD
---
# lessonNN

> textbook-driver course: this file is preparation and routing material, **not the canon of the teaching
> body**. The teaching body is written by `70_tools/canon_append.py` into this directory's
> `teaching_log.md` (the canon) + `emissions.jsonl` (the event ledger); the contract is in
> `50_playbook/canon_carrier.md`. A non-textbook course may delete this note.

## Study scope

- Source: SOURCE_SCOPE
- Exact stop: START_POSITION

## Lesson opening overview

- Before the session, write this lesson's goal and learning path from the textbook's table of contents,
  the full Lesson scope, and the `LessonMap`; it is a route overview only, and must never be passed off as
  teaching from the source material or as the student's confirmation.

## Opening knowledge tree

```text
lessonNN
└─ fill in the character knowledge tree from this lesson's active lesson blocks
```

## Teaching record

- Appended once real teaching starts; a student confirmation must never be pre-created.

## Message-record routing

This section is only a pointer: the sole owner of the routing table is
`00_core/learning_activity_model.md` §2.3 (this Lesson uses the **Lesson variant**). A template or an
instance must never copy the table body, so that two bodies cannot drift.

## Gate ledger

ledger_since: YYYY-MM-DD | starting block: FIRST_CHECKPOINT_ID

The sole owner of the rules is `00_core/learning_activity_model.md` §2.4 (this Lesson uses the
**Lesson variant**); this section holds only the anchor and the trace table, and never copies the rule
body.

| Row ID | Block ID | Gate type | Basis of closure | Response to feeling | Verbatim authorization | Consumed at |
|---|---|---|---|---|---|---|
