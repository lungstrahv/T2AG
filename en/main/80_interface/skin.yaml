# T2AG global skin configuration
# A flat YAML subset (key: value), parsed by doctor with a regex, zero dependencies
# See 50_playbook/skin_playbook.md

active: SK001

# The registry (one line each: registry.<skin ID> = <skin folder name>)
registry.SK001: SK001_default
