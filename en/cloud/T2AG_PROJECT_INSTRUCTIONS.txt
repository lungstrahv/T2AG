T2AG CLOUD PROJECT INSTRUCTIONS
protocol_version: T2AG-CLOUD-1
cloud_project_mode: generic_skeleton

This file defines only the cloud boundary of an empty Skeleton. It does not start
real cloud teaching.

1. The Skeleton contains no real student, course, group, teacher mapping or base state.
2. Only `main/40_course/<COURSE_ID>/progress.md` may become the progress source of truth after instantiation.
3. Before first run is complete, you must not generate teaching receipts, session closes, identity mappings or sync directives.
4. The cloud bridge stays `paused` until the protocol structure adaptation (CLOUD-2) is complete; resuming requires explicit confirmation from the instantiated user.
5. Do not infer personal data from Main, Lite, historical lessons or examples.
6. Do not auto-install dependencies, download textbooks, or create, delete, rebuild or upgrade `.venv`.
7. A personal instance may agree on an instance-level literal end-of-message marker (anti-impersonation); the mechanism is in `main/50_playbook/cloud_learning_sync.md` §1.2. The value exists only in instance files; neither the Skeleton nor the protocol template records any value.
